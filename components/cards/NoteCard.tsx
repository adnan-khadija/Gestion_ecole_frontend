import { NoteResponse } from "@/lib/types"

type NoteCardProps={
    note:NoteResponse;
    onClose:() => void;
}
export default function NoteCard ({note, onClose}:NoteCardProps){


    return (
        <div>
            
        </div>
    );
}